<header
	class="flex justify-center items-center bg-no-repeat bg-cover relative z-10 bg-black"
>
	<!--
        bg-[url('/images/estadio.jpeg')]
        <img
		class="absolute z-0 opacity-20 w-full h-auto"
		src="/images/estadio.jpg"
		alt=""
	/>
    -->

	<div
		class="z-10 my-auto mx-auto flex flex-col absolute justify-center items-center"
	>
		<h1 class="text-5xl">AFT</h1>
		<p class="font-bold text-3xl text-center mb-16">
			Soluciones para tu compañia
		</p>
		<span>
			<img class="w-72 p-0 m-0" src="./icons/logo-2.svg" alt="" />
		</span>

		<!-- 		<a class="w-1/3" href="/web-soccer/form"
			><button class="w-full">Join us</button></a
		> -->
		<!-- color-fondo-logo: #091839 -->
	</div>
</header>

<style>
	span {
	}
	img {
		border-radius: 50%;
		animation: pepe 3s linear infinite;
	}
	@keyframes pepe {
		0% {
			filter: drop-shadow(0 0 5px #31e8ff) drop-shadow(0 0 5px #31e8ff)
				drop-shadow(0 0 5px #31e8ff);
		}
		25% {
			filter: drop-shadow(0 0 5px #31e8ff) drop-shadow(0 0 5px #2c3892);
		}
		50% {
			filter: drop-shadow(0 0 5px #2c3892) drop-shadow(0 0 5px #2c3892);
		}
		75% {
			filter: drop-shadow(0 0 5px #2c3892) drop-shadow(0 0 5px #2c3892);
		}
		100% {
			filter: drop-shadow(0 0 5px #2c3892) drop-shadow(0 0 5px #31e8ff)
				drop-shadow(0 0 5px #31e8ff);
		}
	}
	header {
		height: 45rem;
		display: fixed;
		background: url("/images/estadio.jpeg") fixed;
	}
	header::before {
		content: "";
		background-color: #111;
		height: 100%;
		width: 100%;
		opacity: 0.5;
	}
</style>
